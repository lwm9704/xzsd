<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xzsd.pc.sort.dao.FirstSortDao">

    <!--检验是否有重复的类-->
    <select id="countsFirstSort" parameterType="com.xzsd.pc.sort.entity.FirstSortInfo" resultType="int">
        select count(first_sort_id)
        from t_first_sort
        where is_delete = 0
        and first_sort_name = #{firstSortName}
    </select>

    <!--新增一级分类-->
    <insert id="addFirstSort" parameterType="com.xzsd.pc.sort.entity.FirstSortInfo">
        insert into t_first_sort
            (first_sort_id,
            first_sort_name,
            remark,
            subclass_num,
            is_delete,
            creat_by,
            creat_time,
            last_modify_by,
            modify_time,
            version)
        values
            (#{firstSortId},
            #{firstSortName},
            #{remark},
            0,
            0,
            #{creatBy},
            now(),
            #{lastModifyBy},
            now(),
            0)
    </insert>

    <!--一级分类列表-->
    <select id="listFirstSort" parameterType="com.xzsd.pc.sort.entity.FirstSortInfo" resultType="com.xzsd.pc.sort.entity.FirstSortInfo">
        select
            a.first_sortId firstSortId,
            a.first_sortName firstSortName
        from t_first_sort as a
        where is_delete = 0
    </select>

    <!--删除一级分类-->
    <update id="deleteFirstSort" parameterType="java.lang.String">
        update t_first_sort
        set
            is_delete = 1,
            modify_time = now(),
            last_modify_by = #{userId},
            version = version + 1
        where first_sort_id = #{firstSortId}
        and subclass_num = 0
    </update>

    <!--修改一级分类信息-->
    <update id="updateFirstSort" parameterType="com.xzsd.pc.sort.entity.FirstSortInfo">
        update t_first_sort
        set
            first_sort_name = #{firstSortName},
            remark = #{remark},
            version = version + 1
        where first_sort_id = #{firstSortId}
        and version = #{version}
    </update>

    <!--获取一级分类信息-->
    <select id="getFirstSortInfo" parameterType="java.lang.String" resultType="com.xzsd.pc.sort.entity.FirstSortInfo">
        select
            first_sort_name firstSortName,
            remark
        from t_first_sort
        where first_sort_id = #{firstSortId}
        and is_delete = 0
    </select>
</mapper>